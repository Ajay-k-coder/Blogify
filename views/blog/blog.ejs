<%- include("../layouts/boilerplate") %>
<body class="container bg-dark text-white">
    <!-- <div>
        <h1> Hello <%- blog.title %></h1>
        <img src="<%-blog.path %>" class="img-fluid">
        <div class="m-4 text-justify">
            <%- blog.content %>
        </div>
    </div>
    <hr>
    <% if(locals.user) { %>
     

    <div class="container m-5">
        <div class="text-left">
        <form method="post" action=" <%- blog._id %>/comment" class="mt-5 row">
        <label for="email" class="form-label">Comment</label>
        <div class="row">
        <div class="col-8">
            <input type="text" name="content" class="form-control">
        </div>
        <div class="col-2">
            <button class="form-control btn btn-success" >Add</button>
        </div>
        </div>
        
    </form>
        </div>
    </div>
     <% } %>

    
    <hr>
   <div class="container mt-4">
    <h3>Comments (<%= comments.length %>)</h3>
    
    <% comments.forEach(comment => { %>
        <div class="card mb-3 shadow-sm">
            <div class="card-body">
                <div class="d-flex align-items-center  ">
                    <img src="<%= comment.profile_image_url %>" width="30" height="30" class="rounded-circle me-2">
                    <strong><%= comment.full_name %></strong>
                    <small class="text-muted ms-auto"><%= new Date(comment.created_at).toLocaleDateString() %></small>
                </div>
                <p class="card-text"><%= comment.content %></p>
            </div>
        </div>
    <% }) %>
</div>
    <br>
    <br>

</div> -->


<div class="container blog-container">
    <h1 class="blog-title"><%= blog.title %></h1>

   <div class="d-flex align-items-center mb-4 text-muted"> 
        <img src="<%= author[0].profile_image_url %>" class="user-avatar me-2">
        <span>By <strong><a href="/profile/<%= author[0].id %>" class="userLink"><%= author[0].full_name %></a></strong> • <%= new Date(blog.createdAt).toLocaleDateString() %></span>
    </div> -

    <% if (blog.path) { %>
        <img src="<%= blog.path %>" class="blog-image shadow-sm">
    <% } %>

    <div class="blog-content">
        <%= blog.content %>
    </div>

    <hr class="my-5">

    <section id="comments">
        <h3 class="mb-4">Comments (<%= comments.length %>)</h3>

        <% if (locals.user) { %>
            <form action="<%= blog._id %>/comment" method="POST" class="mb-5">
                <div class="input-group">
                    <input type="text" name="content" class="form-control" placeholder="Add a comment..." required>
                    <button class="btn btn-primary" type="submit">Post</button>
                </div>
            </form>
        <% } %>

        <% comments.forEach(comment => { %>
            <div class="comment-card card mb-3 shadow-sm p-3">
                <div class="d-flex justify-content-between align-items-center mb-2">
                    <div class="d-flex align-items-center">
                        <img src="<%= comment.profile_image_url %>" class="user-avatar me-2">
                        <span class="fw-bold"><%= comment.full_name %></span>
                    </div>
                    <small class="text-muted"><%= new Date(comment.created_at).toLocaleDateString() %></small>
                    <% if (locals.user && locals.user.id === comment.user_id) { %>
                    <form  method="POST" action="<%= blog._id %>/comment/<%= comment.commentId %>?_method=DELETE" onsubmit="return confirm('Delete this comment?')">
                        <input type="hidden" name="blogId" value="<%= blog._id %>">
                        <button type="submit" class="btn btn-sm btn-outline-danger border-0">
                            <i class="bi bi-trash"></i> Delete 
                        </button>
                    </form>
                <% } %>
                </div>
                <p class="mb-0 text-secondary" style="margin-left: 48px;"><%= comment.content %></p>
            </div>
            <hr>
          
        <% }) %>
    </section>
</div>

    
</body>